# ---- core ----
anthropic>=0.45.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
PyYAML>=6.0
numpy>=1.24.0

# ---- audio in ----
sounddevice>=0.4.6          # mic capture (ships wheels, no PortAudio build needed)
faster-whisper>=1.0.0       # local speech-to-text
openwakeword>=0.6.0         # "Hey Jarvis" wake word (pre-trained model included)
onnxruntime>=1.16.0         # inference backend for openwakeword

# ---- audio out ----
edge-tts>=6.1.0             # free neural TTS, no API key, good British voices

# ---- system awareness / tools ----
psutil>=5.9.0               # CPU, RAM, disk, network, battery
mss>=9.0.0                  # fast screen capture for vision
Pillow>=10.0.0              # image encoding
pyperclip>=1.8.2            # clipboard read/write

# ---- optional, install if you want them ----
# elevenlabs>=1.0.0         # premium voice (set tts.engine: elevenlabs)
# pyttsx3>=2.90             # offline fallback voice, works with no internet
# pycaw>=20230407           # Windows absolute volume control
# comtypes>=1.2.0           # required by pycaw
# GPUtil>=1.4.0             # GPU telemetry on the HUD
